<Window x:Class="editor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:editor"
        mc:Ignorable="d" WindowStartupLocation="CenterScreen"
        KeyDown="Window_KeyDown" Closing="Window_Closing" Loaded="Window_Loaded"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Menu>
            <MenuItem Header="文件 (_F)">
                <MenuItem Header="打开 (_O)" Click="MenuFileOpen_Click"/>
                <MenuItem Header="保存 (_S)" Click="MenuFileSave_Click"/>
                <MenuItem Header="另存为 (_A)" Click="MenuFileSaveAs_Click"/>
                <Separator/>
                <MenuItem Header="查看 JSON (_C)" Click="MenuFileJson_Click"/>
                <Separator/>
                <MenuItem Header="退出 (_E)" Click="MenuFileExit_Click"/>
            </MenuItem>
            <MenuItem Header="关于 (_A)" Click="MenuAbout_Click" />
        </Menu>
        <Grid Grid.Row="1" Margin="10,5,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid HorizontalAlignment="Left" Width="150" Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" VerticalAlignment="Top" Margin="0,0,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center">资源</TextBlock>
                    <Button x:Name="ButtonReplaceMedia" Grid.Column="1" Width="35" Height="20" Margin="5,0,0,0" IsEnabled="False" Click="ReplaceMedia_Click">替换</Button>
                    <Button Grid.Column="2" Width="25" Height="20" Margin="5,0,0,0" Click="AddMedia_Click">+</Button>
                    <Button Grid.Column="3" Width="25" Height="20" Margin="5,0,0,0" Click="DelMedia_Click">-</Button>
                </Grid>
                <ListView x:Name="MediaList" Grid.Row="1" ScrollViewer.VerticalScrollBarVisibility="Visible" SelectionChanged="MediaList_SelectionChanged" KeyDown="MediaList_KeyDown" />

            </Grid>
            <Grid x:Name="PageContainer" Grid.Column="1" Margin="10,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Bottom" FontSize="36" Foreground="#808080" Margin="0,0,0,5">未打开文件</TextBlock>
                    <TextBlock Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="14" Foreground="#232323">在左上角“文件”处打开歌姬/音轨配置</TextBlock>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>

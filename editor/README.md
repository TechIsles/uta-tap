# uta-tap 编辑器

使用图形化界面编辑 uta-tap 的歌姬/音轨配置文件。

## 发布到本地
```shell
dotnet publish editor.csproj -c Release -f net8.0-windows -r win-x64 --sc false -o ./publish -p:Platform=x64 -p:PublishSingleFile=true
```
